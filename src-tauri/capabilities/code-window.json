{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "code-window",
  "description": "Capabilities for popped-out code canvas windows (label `code-*`). They need IPC to fetch their initial payload, listen for live `code-window:update` events, emit `code-window:closed` on unload, and close themselves via the header button.",
  "windows": ["code-*"],
  "permissions": ["core:default", "core:window:allow-close"]
}
