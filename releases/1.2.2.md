- **Local-network MCP servers.** MCP server URLs that point at `localhost` or a private LAN address are now accepted, so a self-hosted server on your own machine or network connects instead of being rejected. Only link-local (cloud-metadata) addresses stay blocked.
- **New starter model.** Added `gemma4:12b` to the Ollama catalog in onboarding process.
- Assorted security and stability hardening under the hood.
